created QUOTES using mkdir
created two folder using mkdir..namely frontend and backend. 
installed...npm create vite@latest in frontend...Project name: frontend

backend initialise...
npm init -y
file bna lete hai app.js..ye main file ho jayegi
npm i express nodemon mongoose
npm start

//-----------------------------------------------------------------------
useMemo hook m...values ko yaad kar lete hai..to memoise values...
useMemo( func(){}, [])...accepts 2 things...1st is function and 2nd is dependency array

useCallback()...ye bhi useMemo ke jaisa hi hai bss fark hai ki ye functions ko yaad rakhte hai so that it doesn't recomputing it...
useCallback(fun(){}, [])...it also accepts 2 things..1 is function and 2nd is dependency array.

useRef()...it's also a hook used for dom manipulation...already dom m text likha hua hai and change karna chahte hai toh useRef hook ka use karta hai.
useRef()...element(h1,h2,h3..jo bhi dom ke andar hai) ka reference provide kar deta hai...and reference aa gya toh change kar sakte hai...dom manipulation kar sakte hai.

majorly 2 hi hooks use hote hai... useState, useEffect...baki sab interview perspective se important hai.


Let's go to frontend...
As we all know...frontend m routing naam ki chiz nai hoti..react-router-dom ka use karna padega
ise use karne ke liye we'll have to install it.
npm i react-router-dom .....is command se install kar lenge 
npm i axios

//----------------------------------------------

backend m apis naam ka bnayenge folder...pehle routes naam ka bnate the..we did this means routes naam ka bnaya tha..
apis and routes m difference ?
api se data milta hai...and routing se pages wgr milte hai..toh pages wala kaam toh frontend karega...backend se toh data bhejna hai

data base connect kar lete hai..
mongoosejs.com..



google... staus code success


cmd m check kiya...gian wale m...db m chize ja chuki hai
now we need to show data...

chahte to useEffect se bhi call kar ke...api call kar ke...bhi data ko la sakte hai


frontend m port naam ka component bna rahe hai...port dikhega kaise uske liye...
quotes naam ka folder bnaya component m


CORS: CROSS ORIGIN RESOURCE SHARING...Ye error aya..means backend chal rha hai 8000 pe...frontend chal rha hai 5173 pe..so CORS policy kehti hai 2 alag-alag origin pe data nahi mangwa sakte...
cors error resolve karne ke liye... npm cors..package install karna padega
backend permission dega
google npm cors..
backend m karenge cuz backend se data mangwaya jata hai toh backend permission dega...toh backend m install karenge...npm i cors

.env m bhi nai dikhega api ?


//
Now the task is to basically get a new form..and form ko load karna hai taki new quote bna paye...

frontend se backend m data bhejna hai toh frontnd se api call karni padegi


//-----------------------------------
naviagte...
Agr kisi link pe jana hai...kisi route pe hit karna hai..toh anchor tag to hota hi hai but..anchor 
tag ka ek alternative hota hai useNavigate naam ka hook...
useNavigate()..ye hook react-router-DOM ka hook hai....jo link ki trah same kaam karta hai...kisi url pe bhejne ke liye

1st use case of useRef() hook is...to maniplate DOM.
2nd use case of useRef()
input lena hai us input lene ke liye useRef() hook ka use karenge...ye useRef hame use input ki value ko refer karne ke kaam ayega
and is case m rerendering nai hogi...and useState se same kaam kare toh rerendering ho jayegi
NewQuote.jsx m...


react m routing karne se pehle and baad m ek chiz ka dhyan rakhna hota hai ki it should be a SPA... react refresh na kare

