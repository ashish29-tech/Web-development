<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">

</head>
<body>

  <h1>Edit Product Form</h1>
  
   <!-- foundProduct ke andar se id mil jayegi....<%=foundProduct.name%>..iske aage ?_method=PATCH..but ye method tab ana tha jab ham method override ko install karte hai. Google "npm method override"..open npmjs.com wala link and see kaise install karna hai.."npm i method-override". -->
  <form class="needs-validation" action="/products/<%=foundProduct._id%>?_method=PATCH" method="POST" novalidate>
    <div>
      <label for="naam">Name: </label>
      <!-- Ye form m pehle se value bhi dikhni chahiye....means jab edit karenge toh sara thori na likhne baithenge -->
      <input type="text" name="name" id="naam" value="<%=foundProduct.name%>" required> <!-- foundProduct m pehle se kuch pre-written value nazar ati hai. Input ke andar ek attribute hota hai value(jab DOM kar rahe the tab btaya tha bhaiya ne)...toh isme set kar diya foundProduct.name toh pehle se value mil jayegi-->
    </div>
    <div>
      <label for="pic">Image: </label>
      <input type="text" name="img" id="pic" value="<%=foundProduct.img%>">
    </div>
    <div>
      <label for="daam">Price: </label>
      <input type="text" name="price" id="naam" value="<%=foundProduct.price%>" required>
    </div>
    <div>
      <label for="des">Description: </label>
      <textarea name="desc" id="des" rows="3"> <%=foundProduct.name%> </textarea>
    </div>
    <button type="submit">Edit Product</button>
  </form>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>

  <!-- pura js bootstrap...form...validation se copy kiya hai...isse validation -->
  <script>
    (() => {
  'use strict'

  // Fetch all the forms we want to apply custom Bootstrap validation styles to
  const forms = document.querySelectorAll('.needs-validation')

  // Loop over them and prevent submission
  Array.from(forms).forEach(form => {
    form.addEventListener('submit', event => {
      if (!form.checkValidity()) {
        event.preventDefault()
        event.stopPropagation()
      }

      form.classList.add('was-validated')
    }, false)
  })
})()
  </script>
  
</body>
</html>