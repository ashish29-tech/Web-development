<%-include('../partials/header')%> <%-include('../partials/navbar')%>
<%-include('../partials/flash')%>

<section class="container">
  <h1 class="display-6 mt-5">MY CART</h1>

  <div class="row">
    <div class="col-lg-8">
      <!--cart.js m se user ke naam se bheja hai... toh yha user ke andar jo cart hai uspe loop -->
      <% for(let item of user.cart){ %>
      <div class="card mx-auto m-3 border" style="max-width: 500px">
        <div class="row g-0">
          <div class="col-4">
            <img class="img-fluid rounded-start" src="<%=item.img%>" alt="" />
          </div>
          <div class="col-8">
            <div class="card-body">
              <h5 class="card-title"><%=item.name%></h5>
              <p class="card-text"><%=item.desc%></p>
            </div>
          </div>
        </div>
      </div>
      <% } %>
    </div>
    <div class="col-lg-4">
      <!-- price bhi dikhana hai -->
      <ul>
        <!-- multiple list item bane oadenge...har ek item ke naam ke corresponding uska price dikha sake isliye loop -->
         <!-- Array kya hai user.cart..and cart m kya hai pura ka pura product...tabhi niche item.name..item.price kar pa rahe hai -->
        <% for(let item of user.cart){ %>
        <li><span> <%=item.name%> --&gt; $ <%=item.price%> </span></li>
        <% } %>
        <!-- total amount cart.js se bhej diya hai toh yha access kar sakte hai -->
        <li>Total Amount: $ <%=totalAmount%></li>
      </ul>
      <button class="btn btn-sm btn-success">Checkout</button>
    </div>
  </div>
</section>

<%-include('../partials/footer')%>